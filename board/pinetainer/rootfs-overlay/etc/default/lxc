# Valores de configuración para /usr/libexec/lxc/lxc-containers

# Si true, se iniciarán contenedores con
# la propiedad lxc.start.auto al arrancar la Pine
LXC_AUTO=true

# Grupos de contenedores a arrancar, por orden
BOOTGROUPS="pinetainer"

# Segundos a esperar antes de detener forzosamente los contenedores
SHUTDOWNDELAY=5

# Opciones para iniciar contenedores con lxc-autostart
OPTIONS=

# Opciones para detener contenedores con lxc-autostart
STOPOPTS="-a -A -s"

# No inicializar la red con scripts de LXC
# (ya lo hacemos en ifupdown)
USE_LXC_BRIDGE=false
